spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banking
    username: banking_user
    password: banking_password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true